# 04 — Polish README

**What to build:** README tightened — drops redundant install commands (already in each skill), adds pointer to `skills/shared/setup.md` for the one-time OAuth setup.

**Blocked by:** 01 — Extract shared Cloud Console setup

**Status:** ready-for-agent

- [ ] Redundant `npm install -g` block removed (each skill has its own)
- [ ] Reference to `skills/shared/setup.md` added for OAuth setup
- [ ] Skill table entries consistent with new descriptions from ticket 02
